# Security policy

Do not open a public issue with a database, backup, log file, thread ID, account identifier, or conversation title. Those files can contain private data.

For a security issue, contact the maintainers privately through the repository's security advisory channel. Include a minimal reproduction using a synthetic SQLite database where possible.
